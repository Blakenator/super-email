{
  "name": "@main/email-client",
  "version": "1.0.0",
  "description": "A self-hosted email client with IMAP/POP and SMTP support",
  "main": "index.js",
  "scripts": {
    "start": "pnpm run --parallel -r start",
    "stop": "pnpm run --parallel -r stop",
    "typecheck": "pnpm run --parallel -r typecheck",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down"
  },
  "keywords": ["email", "imap", "smtp", "client"],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.18.2",
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "~5.9.3"
  }
}
